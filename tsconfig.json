{
  "compilerOptions": {
    "target": "ES2020", // 出力するJavaScriptのバージョン（ES2020を使う）
    "module": "commonjs", // モジュールの方式。Electron (Node.js) ではCommonJSが標準
    "strict": true, // 厳格な型チェックをすべて有効にする
    "moduleResolution": "node", // Node.js風にimport解決する（ファイルやnode_modulesから）
    "esModuleInterop": true, // ES Modulesとの互換性を有効にする（default importを許容）
    "forceConsistentCasingInFileNames": true, // ファイル名の大文字・小文字の不一致をエラーにする
    "skipLibCheck": true, // 型定義ファイル（@types/〜）のチェックをスキップ（高速化）
    "outDir": "./dist", // コンパイル後の出力先ディレクトリ
    "baseUrl": ".", // 相対パスの基準（importのaliasなどに使える）
    "jsx": "react-jsx", // React.JSX.Elementでコンポーネントを定義するため
    "lib": ["dom", "esnext"]
  },
  "include": ["main", "renderer", "types", "webpack.renderer.js"]
}
